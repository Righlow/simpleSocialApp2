# simpleSocialApp2
 working version of the Simple Social app with the admin login and profile features
Session-based authentication using express-session with middleware to control access for logged-in users and admin-only routes.

MongoDB with Mongoose for storing users and posts, using schemas, async database operations, and secure user data handling.

EJS server-side rendering, converting HTML pages into dynamic templates that display user data, admin controls, and profile information.
